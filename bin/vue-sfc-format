#!/usr/bin/env bash
# Vue SFC Format CLI wrapper
# Uses nvim's built-in Lua interpreter to run the formatter

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
LUA_SCRIPT="$SCRIPT_DIR/vue-sfc-format.lua"

exec nvim -l "$LUA_SCRIPT" "$@"
